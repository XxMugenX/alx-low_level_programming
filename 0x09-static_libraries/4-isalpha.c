#include "main.h"
#include <stdio.h>
#include <ctype.h>
/**
 *_isalpha - returns true for an alphabet
 *
 *@c: character
 *Return: zero
 */
int _isalpha(int c)
{
if (isalpha(c))
{
return (1);
}
else
{
return (0);
}
return (0);
}
