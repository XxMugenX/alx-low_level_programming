#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 *main - entry
 *@argv: string array
 *@argc: argument count
 *Return: nothing
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
printf("%s\n", argv[0]);

return (0);
}
